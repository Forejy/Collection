if errors
  section
    h2 Errors
    each error in errors
      div
        span=error.param + ": "
        span=error.msg
if flash

if infos && infos.length > 0
  section
    h2 Flash Info
    each info in infos
      div=info
if successes && successes.length > 0
  section
    h2 Flash Success
    each success in successes
      div=success
